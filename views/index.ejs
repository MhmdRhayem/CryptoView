<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
      crossorigin="anonymous"
    />
    <link rel="stylesheet" type="text/css" href="styles/main.css" />
    <title>Blockchain</title>
  </head>
  <body>
    <h1 class="text-center">Block chain project</h1>

    <div class="container">
        <form class="mb-4" style="width: 18rem;" action="/submit" method="post">
            <div class="mb-3">
                <label for="symbol" class="form-label">Blockchain Coin Symbol</label>
                <input type="text" class="form-control" name="symbol" placeholder="Enter coin symbol">
            </div>
            <button type="submit" class="btn btn-primary">Submit</button>
        </form>

        <% if (locals.data) {%>
        <div class="card text-center" style="width: 18rem;">
            <div class="card-header">
                <%= data.symbol %>
            </div>
            <div class="card-body">
                <p class="card-text">Price: <%= data.price_24h %></p>
                <p class="card-text">Volume: <%= data.volume_24h %></p>
                <p class="card-text">Last Trade Price:  <%= data.last_trade_price %></p>
            </div>
        </div>
        <% } else if (locals.err) {%>
        <div class="alert alert-danger" role="alert">
            <%= err %>
        <% } %>
  </body>
</html>
